Make a way for us to upload an image, convert it into a grid based density and have the model minimize the deviation from there
Explore curriculum learning and different ways to prevent the AI from messing up
Have max force be a learnable param and have the env accept -1 to 1 and max force - that way it can modulate based on ambient temp
MUCH LONGER TRAINING RUNS